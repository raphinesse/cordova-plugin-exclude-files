language: node_js
dist: xenial
sudo: false

# Use supported versions https://github.com/nodejs/Release#release-schedule
node_js:
  - 6
  - 8
  - 10
  - 12

# Keep the npm cache around to speed up installs
cache:
  directories: $HOME/.npm
